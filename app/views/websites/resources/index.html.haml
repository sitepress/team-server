%h1 Pages

=link_to "Back to website", @website

%p
  There are
  =@website.resources.size
  pages on the website at

%table
  %thead
    %tr
      %th Page
      %th
  %tbody
    - @website.resources.each do |resource|
      %tr
        %td= resource.file_path
        %td= link_to "Edit", edit_website_resource_path(@website, resource)